class DistanceResult {
  final String distance, time;
  final int distanceM, timeSec;

  DistanceResult({
    required this.distance,
    required this.time,
    required this.distanceM,
    required this.timeSec,
  });
}